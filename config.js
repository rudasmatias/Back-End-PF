

// // ? crypto.randomBytes(32) genera 32 bytes de datos aleatorios y toString("hex") convierte esos bytes en una cadena hexadecimal legible.

// const crypto = require("crypto");

// const secretKey = crypto.randomBytes(32).toString("hex");
// console.log(secretKey);

// module.exports = {
//   secretKey,
// };

